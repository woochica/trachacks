$filter.name (id: $filter.id)

{%     if new_tickets %}\
$new_ticket_hdr $new_tickets_count
{%     end %}\
{%     if changed_tickets %}\
$changed_ticket_hdr $changed_tickets_count 
{%     end %}\

{%     if new_tickets %}\
$filter_sep
$new_tickets_count $new_ticket_hdr
$new_tickets
{%     end %}\

{%     if changed_tickets %}\
$filter_sep
$changed_tickets_count $changed_ticket_hdr
$changed_tickets
{%     end %}\


--
$change_hint
$project.name <${project.url or abs_href()}>
Generated by $sys_desc