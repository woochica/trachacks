from traclegos.project import TracProject
from paste.script import templates

var = templates.var

class ${project}(TracProject):
    _template_dir = 'template'
    summary = ${repr(description)}
    vars = [ ] # variables needing interpolation
    requirements = [ ] # requirements needing to be poached
